# HTTP API reference

The Airflow 3.2.x-compatible HTTP API (`/api/v2/*`) and the UI-support API
(`/ui/*`), rendered from the OpenAPI spec with [Scalar](https://scalar.com).

<div id="api-reference" data-url="openapi.yaml"></div>
<script src="https://cdn.jsdelivr.net/npm/@scalar/api-reference"></script>

!!! note
    The running control plane also serves this live at `/docs` (Scalar) and the
    raw spec at `/openapi.yaml` / `/openapi.json`.
