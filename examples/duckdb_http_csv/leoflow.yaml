schema_version: "1.0"
dag_id: duckdb_http_csv
description: DuckDB reads a remote CSV over HTTP and aggregates it (out-of-core).
owner: examples
tags:
  - example
python_version: "3.11"
dependencies:
  - duckdb==1.4.4
