---
title: Python runtime API
linkTitle: Python API
weight: 50
description: Reference for the Python task runtime (generated).
---

{{% alert title="Placeholder — generated in a later phase" color="warning" %}}
The Python runtime API reference is **not** built by this Hugo site directly. On the
live MkDocs site it is rendered by `mkdocstrings` from the docstrings under
`runtime/python`.

Per the migration plan, the Hugo site will consume this reference from a **sidecar
generator** (pdoc or sphinx) in a later phase, which emits static HTML that this
site embeds. This page is a deliberate placeholder so the nav slot and URL
(`/docs/python-api/`) exist now; do not hand-author API content here.
{{% /alert %}}

Until the sidecar lands, the authoritative Python API reference is on the
[live documentation site](https://neochaotic.github.io/leoflow/python-api.html).
