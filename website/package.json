{
  "name": "leoflow-website",
  "version": "0.0.1",
  "private": true,
  "description": "Leoflow documentation site (Hugo + Docsy). Parallel migration track — not yet deployed.",
  "scripts": {
    "build": "hugo --gc --minify",
    "build:preview": "hugo --gc --minify --environment development"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "postcss-cli": "^11.0.0"
  }
}
